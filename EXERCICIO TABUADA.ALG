Algoritmo "exercicio tabuada"

Var

 n, i, produto : inteiro

Inicio

  escreva("Deseja a tabuada para qual valor? ")
  leia(n)
  
  para i de 1 ate 10 faca

  produto <- n * i
    escreval(n, " X ", i, " = ", produto)
  
  fimpara

Fimalgoritmo